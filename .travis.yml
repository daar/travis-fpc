
language: generic
sudo: required
dist: trusty

os:
  - linux

install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq -y fpc

script:
  - fpc hello.pp 
  - ./hello.pp

notifications:
  email:
    on_success: false
    on_failure: change
